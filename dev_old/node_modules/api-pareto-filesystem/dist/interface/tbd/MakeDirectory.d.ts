import * as pt from "pareto-core-types";
import { TMkdirError } from "../types/MkdirError";
import { Path } from "../types/Path";
import { FSResult, TAnnotatedFSError } from "./FSResult";
export declare type Mkdir_Data = {
    readonly path: Path;
    readonly createContainingDirectories: boolean;
};
export declare type Mkdir_Result = FSResult<TAnnotatedFSError<TMkdirError>, {}>;
export declare type Mkdir = ($: Mkdir_Data) => pt.AsyncValue<Mkdir_Result>;
