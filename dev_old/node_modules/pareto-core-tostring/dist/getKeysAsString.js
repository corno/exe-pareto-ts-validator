"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.getKeysAsString = void 0;
function getKeysAsString(dict) {
    const maxAmount = 10;
    const foundKeys = [];
    dict.forEach(() => false, ($, key) => {
        foundKeys.push(key);
    });
    const formatted = foundKeys.map($ => `'${$}'`);
    if (foundKeys.length > maxAmount) {
        formatted.splice(maxAmount);
        formatted.push("...");
    }
    return `${foundKeys.join(", ")}`;
}
exports.getKeysAsString = getKeysAsString;
